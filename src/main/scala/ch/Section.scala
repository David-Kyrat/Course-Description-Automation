/* package ch

/**
 * Represents a Section / Department, i.e. an entity inside a Faculty
 */
// trait Section {} //TODO implement section, see if make an enum or a case class in case, new section may appear */
